[build]
  publish = "."
  functions = "netlify/functions"

# /api/enhance is declared by the function's own `config.path` so that its
# `rateLimit` rule applies; a redirect here would bypass that.

[functions]
  node_bundler = "esbuild"
